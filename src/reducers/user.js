const USER_STATE = {
  accesstoken: "2c932329-71cc-40ee-a82a-0ad9df7ec66d" //用户秘钥
};

export default function user(state = USER_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
